This folder contains an ever-so-slightly modified version of the subset of
[deno's std lib](https://github.com/denoland/std) required to get the node
polyfills working.

New code is marked with `// @wasmer`.
