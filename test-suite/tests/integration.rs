use libtest_mimic::Arguments;
use test_suite::TestManager;

fn main() -> Result<(), anyhow::Error> {
    let args = Arguments::from_args();

    // let mut manager = TestManager::new()?;
    // manager.run_tests()?;
    Ok(())
}
